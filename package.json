{
  "name": "bnbflow-studio",
  "version": "3.0.0-prototype",
  "private": true,
  "description": "Prototipo statico del runtime outcome-first per hospitality operations",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check catalog.js && node --check case-data.js && node --check runtime.js && node --check mock-tools.js && node --check scenarios.js && node --check task-catalog.js && node --check policies.js && node --check audit.js && node --check storage.js && node --test tests/*.test.js"
  }
}
